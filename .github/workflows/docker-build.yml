name: Build and Push All Microservices

on:
  push:
    branches:
      - main

jobs:

  user-service:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: UserService

    steps:
      - name: Checkout Code
        uses: actions/checkout@v5

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build Product Service
        run: mvn clean package -DskipTests

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker Image
        run: docker build -t satishbabu1989/user-service:latest .

      - name: Push Docker Image
        run: docker push satishbabu1989/user-service:latest


  product-service:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ProductService

    steps:
      - name: Checkout Code
        uses: actions/checkout@v5

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build Product Service
        run: mvn clean package -DskipTests

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker Image
        run: docker build -t satishbabu1989/product-service:latest .

      - name: Push Docker Image
        run: docker push satishbabu1989/product-service:latest

  order-service:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: OrderService

    steps:
      - name: Checkout Code
        uses: actions/checkout@v5

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build Order Service
        run: mvn clean package -DskipTests

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker Image
        run: docker build -t satishbabu1989/order-service:latest .

      - name: Push Docker Image
        run: docker push satishbabu1989/order-service:latest

  payment-service:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: PaymentService

    steps:
      - name: Checkout Code
        uses: actions/checkout@v5

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build Payment Service
        run: mvn clean package -DskipTests

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker Image
        run: docker build -t satishbabu1989/payment-service:latest .

      - name: Push Docker Image
        run: docker push satishbabu1989/payment-service:latest

  api-gateway:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ApiGateway

    steps:
      - name: Checkout Code
        uses: actions/checkout@v5

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build API Gateway
        run: mvn clean package -DskipTests

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker Image
        run: docker build -t satishbabu1989/api-gateway:latest .

      - name: Push Docker Image
        run: docker push satishbabu1989/api-gateway:latest

  config-server:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ConfigServer

    steps:
      - name: Checkout Code
        uses: actions/checkout@v5

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build Config Server
        run: mvn clean package -DskipTests

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker Image
        run: docker build -t satishbabu1989/config-server:latest .

      - name: Push Docker Image
        run: docker push satishbabu1989/config-server:latest

  service-registry:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ServiceRegistry

    steps:
      - name: Checkout Code
        uses: actions/checkout@v5

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build ServiceRegistry
        run: mvn clean package -DskipTests

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker Image
        run: docker build -t satishbabu1989/service-registry:latest .

      - name: Push Docker Image
        run: docker push satishbabu1989/service-registry:latest