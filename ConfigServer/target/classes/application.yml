server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/satishbabu7777/eommoerce_config_repo
          default-label: main
          clone-on-start: true